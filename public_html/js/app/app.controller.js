(function () 
{
    "use strict";

    angular.module('app')
            .controller('AppController', AppController);
//            .animation('.view-animate', ViewChangeAnimation);

    AppController.$inject = ['$log', '$scope', '$rootScope'];

    function AppController($log, $scope, $rootScope)
    {
        var vm = this;

        (function init()
        {
            
        })();

        ///////////////////////////////////////////////////////////////////////////////////////////////////
    }
})();
